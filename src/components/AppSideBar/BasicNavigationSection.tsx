import React from "react";

interface BasicNavigationSectionProps {
  children: React.ReactNode;
}
const BasicNavigationSection = ({ children }: BasicNavigationSectionProps) => {
    
  return children;
};

export default BasicNavigationSection;
